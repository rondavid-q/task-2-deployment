version: '3.7'

services:
  environment:
    container_name: environment
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 8000:8000
    stdin_open: true
    environment:
      - CHOKIDAR_USEPOLLING=true
    command: node app.js
